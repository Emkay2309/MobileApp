import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    backIconContainer: {
      position: 'absolute',
      top: 40,
      left: 20,
    },
    backIcon: {
      color: 'black',
    },
    headingContainer: {
      display: 'flex',
      alignContent: 'center',
      alignItems: 'center',
    },
    instruction: {
      // Add necessary styles
      //color: 'black',
    },
    textStyle: {
      // Add necessary styles
      // color: 'black',
    },
    headingText: {
      fontSize: 40,
      fontWeight: '700',
      color: 'black',
    },
    gradient: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      paddingTop: 20, // Add padding to make space for the back icon
    },
    container: {
      marginTop: 50,
    },
  });
  